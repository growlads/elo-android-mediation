# Changelog

This repository ships first-party Elo Android mediation adapters as separate Gradle modules. Each adapter has its own version and per-adapter detail lives next to the adapter source.

For per-adapter changelogs, see:

- **AdMob** — [`adapter-admob/CHANGELOG.md`](adapter-admob/CHANGELOG.md)

The summary below aggregates significant changes across adapters.

## Unreleased

## 2026-05-01

- **AdMob 0.0.2** — `AdMobNativeAdRenderer` card chrome redesign (rounded surface, padded layout, Sponsored badge, larger icon, prominent rounded CTA), honor AdMob's `callToAction` casing, hide CTA when blank, add localizable `sponsoredLabel` parameter.
- **AdMob 0.0.1** — Initial release.
